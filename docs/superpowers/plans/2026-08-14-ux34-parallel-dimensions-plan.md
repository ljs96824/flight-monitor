# UX 3.4 TDD 实施计划

1. 新增失败契约：场景列表、同行约束列表、`companions` 派生、推荐依据完整串。
2. 新增失败契约：route_type 无手动选择器、徽章存在、`/price_hint` 返回自动分类。
3. 新增失败契约：显隐白名单恰为三项，商务组常驻 DOM，编辑商务时显示并展开。
4. 新增第十个“多场景 + 老幼同行”规范化夹具，保持前九份 JSON 不变。
5. 最小修改 `form_pages.py` 与 `web_form.py`，再扩展 `ui_smoke_driver.mjs` 的商务显隐转换。
6. 运行定向测试、快照 diff、双收集器、隔离端口 UI smoke、邮件离线复放和台账哈希核对。
7. 检查变更范围后创建一个独立提交，不推送。
