# P7-A/B 预测门控等价性与只读快照实施计划

**目标：** 保留 P4/P5 已验证统计口径，统一预测资格裁决、锁死同日型不回退，并让预测与 T 曲线报告在固定只读快照上复现。

**边界：** 不修改采集、采样、面板数据、推荐、金额、排序和现有推送业务输出。

## 任务 1：基线与消费路径审计

- [x] 生成改前快照并记录台账、观测库与价格库 SHA-256。
- [x] 审计邮件、PushPlus、网页详情、推荐判定四条消费路径。
- [x] 记录每条路径的 shape 门、技能门和低样本行为。

## 任务 2：统一预测资格裁决（TDD）

- [x] 先写六状态、最短板不可补偿和源码扫描测试。
- [x] 新增 `evaluate_forecast_eligibility(...)` 唯一裁决入口。
- [x] 报告和通知构建器传入真实 shape、技能门、源覆盖、regime 与 lineage 证据。
- [x] 保持预测字段当前不进入统一通知 payload，冻结邮件不变。

## 任务 3：锁死 regime 不回退（TDD）

- [x] 默认只使用目标 regime 的 shape。
- [x] 同 regime 不足返回 `regime_insufficient`。
- [x] `--diagnostic` 仅展示跨 regime 原始候选并标注不可用于判断。

## 任务 4：只读快照与报告目录解析（TDD）

- [x] SQLite 使用 online backup，覆盖 WAL 已提交页并排除未提交页。
- [x] 快照 manifest 固化 SHA 与 PermissionError 质量格清单。
- [x] `forecast_report` 与 `tcurve_report` 的 `--db` 接受完整快照目录。
- [x] 快照复放不读取生产轮档或回退到生产数据库。

## 任务 5：验证与独立提交

- [x] 生成改后快照并确认业务字段仅有预期变化。
- [x] 用新只读快照运行两份报告并与同一时点生产输入对账。
- [x] 运行冻结邮件、双收集器全量测试、diff 检查和独立审阅。
- [x] 复核台账与三库 SHA-256 不变。
- [x] 创建独立提交，不 push。
